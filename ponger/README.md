# ponger
pong server and client implementations, in node.
`npm install` to install packages
`node server.js` to run server (port 8080)
`node client.js <server location> <player number>` to run client
client listens for messages (hit enter to send). use client numbers 0/1 for now
server location- eg "http://localhost:8080"
